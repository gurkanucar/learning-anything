# Java 17 and Maven Install
## Java 17 Installation

```
sudo apt-get update
sudo apt-get install openjdk-17-jdk -y
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
echo $JAVA_HOME
java -version

```
## Maven Installation
```
sudo apt install maven -y
mvn -version
```